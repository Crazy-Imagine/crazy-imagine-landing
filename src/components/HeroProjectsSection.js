import React from "react"
import { Box, Typography } from "@material-ui/core"

const HeroProjectsSection = () => {
  return (
    <Box>
      <Typography>HeroProjectsSection</Typography>
    </Box>
  )
}

export default HeroProjectsSection

import React from "react"
import { Box, Typography } from "@material-ui/core"

const MembersCard = () => {
  return (
    <Box>
      <Typography>Members card</Typography>
    </Box>
  )
}

export default MembersCard

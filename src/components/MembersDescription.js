import React from "react"
import { Box, Typography } from "@material-ui/core"

const MembersDescription = () => {
  return (
    <Box>
      <Typography>MemberDescription</Typography>
    </Box>
  )
}

export default MembersDescription

import React from "react"
import { Box, Typography } from "@material-ui/core"

const AboutProjects = () => {
  return (
    <Box>
      <Typography>AboutProjects</Typography>
    </Box>
  )
}

export default AboutProjects

import React from "react"
import { Box, Typography } from "@material-ui/core"

const MoreProjects = () => {
  return (
    <Box>
      <Typography>MoreProjects</Typography>
    </Box>
  )
}

export default MoreProjects
